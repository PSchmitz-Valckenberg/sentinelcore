# .env.example — copy to .env and run: source .env
# Spring Boot alternative: set LLM_API_KEY in application-local.yml using application-local.yml.example as the template
LLM_API_KEY=your-gemini-api-key-here